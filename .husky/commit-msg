#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Block cross-org closing refs (private-tracker leak into the public CHANGELOG).
sh "$(dirname "$0")/../scripts/guard-commit-cross-org-refs.sh" "$1" || exit 1
npx --no-install commitlint --edit "$1"
