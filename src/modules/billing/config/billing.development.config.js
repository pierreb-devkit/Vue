/**
 * @deprecated Renamed to billing.static-content.js. The `plans` and `packs` named
 * exports were removed in #4654 — `tabs` is the replacement; there is no
 * compatibility bridge for the old names.
 * Import from '../config/billing.static-content' instead.
 */
export { tabs, default } from './billing.static-content.js';
