/**
 * Per-module project override template — home module.
 *
 * Copy and rename to home.{yourproject}.config.js, then run with NODE_ENV={yourproject}.
 * Override only the keys that differ from home.development.config.js defaults.
 *
 * Load order (this file is layer 3 — above module defaults, below global project config):
 *   1. home.development.config.js  (module defaults)
 *   2. development.config.js       (global dev defaults)
 *   3. home.{project}.config.js    (← this file)
 *   4. {project}.config.js         (global project overrides)
 *   5. DEVKIT_VUE_* env vars
 *
 * @example NODE_ENV=myproject npm run dev
 */
export default {
  // home: {
  //   hero: {
  //     title: 'My Project — <b>Built Different.</b>',
  //     subtitle: 'Short value proposition for myproject.',
  //     button: { title: 'Get Started', color: '#1abc9c', link: '/signup' },
  //   },
  //   contact: {
  //     mail: 'mailto:hello@myproject.com',
  //   },
  // },
};
